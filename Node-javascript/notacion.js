//----------notacion-----------
// ; => delimitar el final de una linea,hoy en dia ya no es necesario
const b = 4;
b + 4;

// . => se utiliza en los objetos para acceder a los atributos
// movil.anchura


// [] => se utiliza para las listas, arreglos, arrays o vectores
const ar = ^[1, 2, 3, 4]
console.log(ar[2]) 

//() => funciones
function suma(a, b){
    //se escribiria la funcion
}

//{} => llaves para objetos, funciones y estructuras de control
const movil = {
    anchura: 5;
    altura: 10;
}

if(true) {
    //todo lo que haya entre llaves
}